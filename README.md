# Extended-Action-Switch
Extends the native Multi Action Switch with a third, hold-activated action tree. Pressing the key toggles between switch states as normal, while holding the key runs a separate Multi Action without changing the current state.
